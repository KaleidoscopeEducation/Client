// import api from 'librechat-data-provider/axios';
// import * as ep from 'librechat-data-provider/api-endpoints';

// // ----------  NEW: wrapper around the backend route  ----------
// export async function runAssignment(
//   consultationId: string,
//   assistantId: string,
//   prompt = '',
// ) {
//   const url = ep.runAssignment(consultationId);   // `/api/consultations/:id/assignments/run`
//   const data = await api.post(url, { assistantId, prompt });
//   // server returns [{ filename, blobUrl, createdAt }]
//   return data;
// }
// // ----------  END NEW  ----------
